# بخش ۱ — معرفی Tuple

> 🌐 Language: **فارسی** | [English](../README.md)

## فهرست مطالب تاپل

| بخش | موضوع |
|---:|---|
| ۱ | معرفی Tuple |
| ۲ | ساخت Tuple |
| ۳ | دسترسی به عناصر Tuple |
| ۴ | برش Tuple |
| ۵ | تغییر ناپذیری Tuple |
| ۶ | بررسی وجود یک عنصر در Tuple |
| ۷ | طول و شمارش عناصر Tuple |
| ۸ | پیدا کردن موقعیت یک عنصر |
| ۹ | پیمایش Tuple |
| ۱۰ | ترکیب و تکرار Tuple |
| ۱۱ | تبدیل List و Tuple |
| ۱۲ | باز کردن Tuple با Unpacking |
| ۱۳ | Tupleهای تو در تو |
| ۱۴ | مرور نهایی Tuples |
| ۱۵ | پروژه کوچک Tuples |

## ۱. Tuple چیست؟

**تاپل** یکی از انواع داده در پایتون است که می تواند چند مقدار را در کنار یکد یگر نگه دارد.

تاپل شبیه لیست است، اما یک تفاوت مهم دارد:

```text
List  → قابل تغییر است
Tuple → بعد از ساخت قابل تغییر نیست
```

برای مثال:

```python
fruits = ("Apple", "Banana", "Orange")

print(fruits)
```

خروجی:

```text
('Apple', 'Banana', 'Orange')
```

مقادیر مختلف درون یک تاپل قرار گرفته اند.

## ۲. ساخت Tuple

برای ساخت تاپل معمولاً عناصر را داخل پرانتز `()` قرار می دهیم.

```python
numbers = (10, 20, 30, 40)

print(numbers)
```

خروجی:

```text
(10, 20, 30, 40)
```

یک تاپل می تواند شامل انواع مختلف داده نیز باشد:

```python
student = ("Ali", 20, 18.5)

print(student)
```

خروجی:

```text
('Ali', 20, 18.5)
```

## ۳. تفاوت Tuple و List

برای ساخت تاپل از براکت استفاده می کنیم:

```python
fruits = ["Apple", "Banana", "Orange"]
```

برای ساخت تاپل معمولاً از پرانتز استفاده می کنیم:

```python
fruits = ("Apple", "Banana", "Orange")
```

تفاوت مهم این دو در تغییر پذیری آن هاست.

List قابل تغییر است:

```python
fruits = ["Apple", "Banana", "Orange"]

fruits[0] = "Mango"

print(fruits)
```

خروجی:

```text
['Mango', 'Banana', 'Orange']
```

اما Tuple را نمی توان به این شکل تغییر داد:

```python
fruits = ("Apple", "Banana", "Orange")

fruits[0] = "Mango"
```

پایتون خطا ایجاد می کند، چون عناصر تاپل بعد از ساخته شدن قابل تغییر نیستند.

## ۴. چه زمانی از Tuple استفاده می کنیم؟

تاپل زمانی کاربردی است که چند مقدار داشته باشیم و بخواهیم این مقادیر ثابت باقی بمانند.

برای مثال:

```python
location = (51.5074, -0.1278)

print(location)
```

خروجی:

```text
(51.5074, -0.1278)
```

این تاپل می تواند یک جفت مقدار ثابت را نمایش دهد.

مثال دیگر:

```python
birthday = (15, 8, 2005)

print(birthday)
```

خروجی:

```text
(15, 8, 2005)
```

## ۵. یک نکته مهم برای شروع

تاپل فقط به معنی «لیست با پرانتز» نیست.

مفهوم اصلی تاپل این است که **تغییر ناپذیر** است.

یعنی بعد از ساخته شدن تاپل نمی توانیم مستقیماً عناصر آن را تغییر دهیم، اضافه کنیم یا حذف کنیم.

در بخش های بعدی این مفهوم را دقیق تر بررسی می کنیم.

فعلاً به خاطر داشته باشید:

```text
List  → قابل تغییر
Tuple → تغییرناپذیر
```

# سوال ها

## سوال ۱

Tuple چیست؟

## سوال ۲

تفاوت اصلی List و Tuple چیست؟

## سوال ۳

آیا این کد معتبر است؟ چرا؟

```python
numbers = (10, 20, 30)

numbers[0] = 100
```

## سوال مروری

بر اساس مطالبی که تا اینجا یاد گرفته ایم، تفاوت یک نوع داده تغییر پذیر و یک نوع داده تغییر ناپذیر چیست؟

# پاسخ ها

## پاسخ ۱

تاپل یک نوع داده در پایتون است که می تواند چند مقدار را در کنار یکد یگر نگه دارد.

## پاسخ ۲

لیست بعد از ساخته شدن قابل تغییر است، اما تاپل بعد از ساخته شدن مستقیماً قابل تغییر نیست.

## پاسخ ۳

خیر. این کد معتبر نیست، زیرا عناصر تاپل بعد از ساخته شدن قابل تغییر نیستند.

## پاسخ سوال مروری

یک نوع داده تغییر پذیر می تواند بعد از ساخته شدن تغییر کند، اما یک نوع داده تغییر ناپذیر بعد از ساخته شدن مستقیماً قابل تغییر نیست.

---

# بخش ۲ — ساخت Tuple

## ۱. ساخت یک Tuple

معمولاً برای ساخت تاپل چند مقدار را داخل پرانتز `()` قرار می دهیم و عناصر را با کاما از هم جدا می کنیم.

```python
fruits = ("Apple", "Banana", "Orange")

print(fruits)
```

خروجی:

```text
('Apple', 'Banana', 'Orange')
```

کاماها مهم هستند، چون عناصر تاپل را از یکدیگر جدا می کنند.

## ۲. ساخت Tuple از اعداد

یک Tuple می تواند شامل اعداد باشد:

```python
numbers = (10, 20, 30, 40)

print(numbers)
```

خروجی:

```text
(10, 20, 30, 40)
```

## ۳. ساخت Tuple با انواع مختلف داده

یک تاپل می تواند شامل انواع مختلف داده باشد.

```python
student = ("Ali", 20, 18.5, True)

print(student)
```

خروجی:

```text
('Ali', 20, 18.5, True)
```

## ۴. ساخت Tuple خالی

می توانیم با استفاده از پرانتزهای خالی یک تاپل خالی بسازیم:

```python
empty_tuple = ()

print(empty_tuple)
```

خروجی:

```text
()
```

یک Tuple خالی هیچ عنصری ندارد.

## ۵. ساخت Tuple تک عنصری

یک نکته بسیار مهم این است که برای ساخت تاپل دارای یک عنصر، به یک کاما نیاز داریم.

این یک Tuple نیست:

```python
number = (10)

print(type(number))
```

خروجی:

```text
<class 'int'>
```

پایتون `(10)` را یک عبارت معمولی از نوع عدد صحیح در نظر می گیرد.

برای ساخت یک تاپل تک عنصری باید کاما قرار دهیم:

```python
number = (10,)

print(type(number))
```

خروجی:

```text
<class 'tuple'>
```

این کاما مهم است.

## ۶. ساخت Tuple بدون پرانتز

پایتون اجازه می دهد بدون نوشتن مستقیم پرانتز نیز تاپل بسازیم.

```python
fruits = "Apple", "Banana", "Orange"

print(fruits)
```

خروجی:

```text
('Apple', 'Banana', 'Orange')
```

پایتون مقادیر جدا شده با کاما را به عنوان یک تاپل تشخیص می دهد.

با این حال، استفاده از پرانتز برای افراد تازه کار معمولاً واضح تر است:

```python
fruits = ("Apple", "Banana", "Orange")
```

## ۷. ساخت Tuple از یک List

می توانیم با استفاده از `tuple()` یک List را به تاپل تبدیل کنیم.

```python
fruits = ["Apple", "Banana", "Orange"]

fruits_tuple = tuple(fruits)

print(fruits_tuple)
```

خروجی:

```text
('Apple', 'Banana', 'Orange')
```

در بخش های بعدی تبدیل بین لیست و تاپل را با جزئیات بیشتری بررسی می کنیم.

## ۸. بررسی نوع داده

با استفاده از `type()` می توانیم بررسی کنیم که یک مقدار از نوع تاپل است یا نه.

```python
colors = ("Red", "Green", "Blue")

print(type(colors))
```

خروجی:

```text
<class 'tuple'>
```

## ۹. قانون مهم

هنگام ساخت Tuple این موارد را به خاطر داشته باشید:

```text
چند عنصر      → عناصر با کاما از هم جدا می شوند
یک عنصر       → قرار دادن کاما ضروری است
Tuple خالی    → ()
```

برای مثال:

```python
a = ()
b = (10,)
c = (10, 20, 30)
```

# سوال ها

## سوال ۱

چگونه یک تاپل می سازیم؟

## سوال ۲

تفاوت این دو چیست؟

```python
a = (10)
b = (10,)
```

## سوال ۳

این برنامه چه چیزی چاپ می کند؟

```python
numbers = 10, 20, 30

print(numbers)
print(type(numbers))
```

## سوال مروری

تفاوت اصلی لیست و تاپل چیست و برای ساخت یک تاپل دارای فقط یک عنصر چه کاری باید انجام دهیم؟

# پاسخ ها

## پاسخ ۱

معمولاً تاپل را با قرار دادن مقادیر جدا شده با کاما داخل پرانتز `()` می سازیم.

## پاسخ ۲

`(10)` یک عبارت عدد صحیح است، اما `(10,)` یک تاپل دارای یک عنصر است.

## پاسخ ۳

خروجی برنامه:

```text
(10, 20, 30)
<class 'tuple'>
```

## پاسخ سوال مروری

لیست تغییر پذیر است، اما تاپل تغییر ناپذیر است. برای ساخت تاپل دارای یک عنصر باید یک کاما قرار دهیم؛ مانند `(10,)`.

---

